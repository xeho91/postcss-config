import presetEnv from "postcss-preset-env"; // eslint-disable-line unicorn/prevent-abbreviations

// https://github.com/csstools/postcss-preset-env
export default presetEnv({
	stage: 1,
	features: {
		// https://preset-env.cssdb.org/features
	},
});
